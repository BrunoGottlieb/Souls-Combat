using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossWeaponHandle : MonoBehaviour
{
    /*public GameObject weapon;
    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.tag == "Enemy" || other.gameObject.tag == "Player")
        {
            other.GetComponent<Rigidbody>().AddForce(this.transform.InverseTransformDirection(other.transform.position) * 100);
        }
    }

    private void OnCollisionEnter(Collision collision)
    {
        if (collision.gameObject.tag == "Enemy" || collision.gameObject.tag == "Player")
            collision.rigidbody.AddExplosionForce(100, collision.transform.position, 5.0f, 3.0F);
    }*/
}
